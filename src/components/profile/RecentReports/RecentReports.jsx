import React from "react";
import {
  ContentContainer,
  ContentTitle,
  ContentText,
} from "../../../commonStyles/styled";

const RecentReports = (props) => {
  return (
    <ContentContainer>
      <ContentTitle>Recent Session Reports</ContentTitle>
      <ContentText>No data currently linked to this profile</ContentText>
    </ContentContainer>
  );
};

export default RecentReports;
